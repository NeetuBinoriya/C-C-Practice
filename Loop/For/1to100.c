#include <stdio.h>
int main()
{
    // int n;
    // printf("Enter a Number for N: ");
    // scanf("%d", &n);

    for (int i = 1; i <= 100; i++)
    {
        printf("%d ", i);
    }
    return 0;
}